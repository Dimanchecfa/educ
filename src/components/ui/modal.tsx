import {
    Sheet,
    SheetContent,
    SheetHeader,
    SheetTrigger,
} from "@/components/ui/sheet"


export default function Modal() {
    return (

        <Sheet>
            <SheetTrigger>Open</SheetTrigger>
            <SheetContent>
                <SheetHeader>

                </SheetHeader>
            </SheetContent>
        </Sheet>

    )
}
